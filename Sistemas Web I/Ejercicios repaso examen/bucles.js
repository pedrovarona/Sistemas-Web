let valores = promt("Introduce un valor a placer, los numeros se sumaran hasta que el valor que introduzcas sea vacio", "");
valoresNum = Number(valores);

while (valoresNum !== ""){
    if (typeof valoresNum === "number"){

    }
}